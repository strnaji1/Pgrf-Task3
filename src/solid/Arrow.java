/*package solid;

import transforms.Col;
import transforms.Point3D;

public class Arrow extends Solid {

    public Arrow() {
        // Vrcholy šipky
        vb.add(new Point3D(0, 0, 0));    // Start
        vb.add(new Point3D(0, 0, 1));    // Konec
        vb.add(new Point3D(-0.1, -0.1, 0.9)); // Levý trojúhelník
        vb.add(new Point3D(0.1, -0.1, 0.9));  // Pravý trojúhelník

        // Indexy
        addIndices(0, 1, 1, 2, 1, 3);

        // Nastavení barvy šipky
        setColor(new Col(0xff0000)); // Červená barva
    }
    }

*/